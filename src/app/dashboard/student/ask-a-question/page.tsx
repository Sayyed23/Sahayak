import AskAQuestionPage from "@/app/ask-a-question/page";

export default function StudentAskAQuestion() {
    // This is a wrapper to render the main "Ask a Question" page
    // within the student's dashboard layout. In a real app,
    // you might have a single page and handle layout contextually.
    return <AskAQuestionPage />;
}
