
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For this prototype, allow read/write access to any authenticated user.
    // In a production app, you should create more granular rules to
    // secure your data.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
